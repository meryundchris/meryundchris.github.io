# Einfache Testseite

Öffne die Datei `index.html` in deinem Browser, oder starte einen einfachen lokalen Server:

Für Node (http-server):

```bash
npx http-server .
```

Oder mit Python 3:

```bash
python -m http.server 8000
```

Dann im Browser öffnen: http://localhost:8000
